name: CI

on:
    push:
        branches:
        - main

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
        - name: Checkout code
          uses: actions/checkout@v2

        - name: Build project
          run: open index.html
            echo "building the project"

        - name: Upload artifact
          uses: actions/upload-artifact@v2
          with:
            name: my-artifact
            path: /home/katsyrina/Рабочий стол/dev/ono-tebe-nado